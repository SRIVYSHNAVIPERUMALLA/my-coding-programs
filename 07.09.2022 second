#include<stdio.h>
#include<conio.h>
void main()
{
  int a[10],*p,n,i,sum=0;
  clrscr();
  printf("Enter n size");
  scanf("%d",&n);
  printf("Enter array elements");
  for(i=0;i<n;i++)
  scanf(i=0;i<n;i++)
  {
    sum=sum+(*p);
    p++;
  }
  printf("%d",sum);
  getch();
  }
  
